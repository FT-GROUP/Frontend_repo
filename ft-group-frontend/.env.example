VITE_API_URL=http://localhost:3000/api
# local = grupos y gastos se guardan en el navegador (el backend aún no tiene esos módulos)
# api   = usa los endpoints REST /api/grupos, /api/gastos, etc.
VITE_FINANCE_MODE=local
